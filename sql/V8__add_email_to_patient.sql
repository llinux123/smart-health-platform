-- ============================================================
-- V8: 患者表新增邮箱字段（用于密码找回）
-- ============================================================

ALTER TABLE `t_patient`
    ADD COLUMN `email` varchar(100) DEFAULT NULL COMMENT '邮箱（密码找回）' AFTER `gender`;
